# Security policy

Report vulnerabilities privately through GitHub's security reporting feature rather than a public issue.

Never commit credentials, `.env` files, private datasets or SSH material. Treat PyTorch checkpoints and other pickle-based artifacts as untrusted unless their provenance and hash are known.

ThermalAI is research software and does not provide safety guarantees for production thermal-control systems.

